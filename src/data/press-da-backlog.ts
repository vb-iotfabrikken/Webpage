/**
 * Danish press mentions — moved to `press.ts` (`mentionSources` / `getPressMentions`).
 * This file is kept as a historical reference only.
 */

export { getPressMentions as mentionsDaBacklog } from "./press";
